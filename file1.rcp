main

#import intrin as *
#import io as *

cast :: fn(t: type, val: $y) -> t {
    return @t val;
}

main :: fn() -> i32 {

    x := v2 { 1.3, 4.3 };
    y := v2 { 1, 2 };

    println("%", x + y);

    return cast(i32, x.y);
}

#run main();

