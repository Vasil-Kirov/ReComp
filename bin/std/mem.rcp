memory

struct Allocator {
    alloc: fn(size: int, this: *) -> *,
    data: *,
}

__raw_slice :: fn #intr (T: type, data: *T, size: int) -> []T;

make_slice :: fn (allocator: *Allocator, T: type, size: int) -> []T {
    return __raw_slice(T, allocator.alloc(size, allocator), size);
}

