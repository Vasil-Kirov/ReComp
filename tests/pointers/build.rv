module build
#import compile as c

compile :: fn() -> c.CompileInfo {
    out := c.CompileInfo {
        files = []string { "pointers.rv" },
        opt = 0,
        flags = .SanAddress,
    };
    return out;
}
