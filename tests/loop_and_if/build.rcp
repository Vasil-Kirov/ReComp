build
#import compile as c

compile :: fn() -> c.CompileInfo {
    out := c.CompileInfo {
        files = []string { "loop_and_if.rcp" },
        opt = 0,
        flags = .SanAddress,
    };
    return out;
}
