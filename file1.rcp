main

#import file2 as f2

struct x {
	bar: string,
	num: int,
}

my_64_fn :: fn() -> i32 {
	return 64;
}

my_16_fn :: fn(f: fn(x: i32) -> i32) -> i32 {
	return f(64);
}

make_x :: fn(num: int) -> x {
	foo := x { bar = "my_string", num = num };
	return foo;
}

main :: fn() -> i32 {
	// my_lambda := fn() -> i32 {
	// 	return 32;
	// };

	// fn_ptr := my_16_fn;
	
	//fn_ptr = my_64_fn;

	
	f := make_x(5);

	return @i32 (#size f.bar - f.num);
}

