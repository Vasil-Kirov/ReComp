module main

#import "libc.rcp" as c
#import intrin as *
#import io as *
#import mem as *

main :: fn() -> i32 {

    alloc := c.make_malloc_allocator(true);
    arr := make_slice(int, 16, &alloc);
    for i, it in arr {
        println("%: %", i, it);
    }

    return 0;
}

#run main();

