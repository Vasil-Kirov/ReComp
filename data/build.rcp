build

#import compile as c

compile :: fn() -> c.CompileInfo {
    out := c.CompileInfo {
        files = []string { "../file1.rcp", "../file2.rcp" },
        opt = 2,
        flags = @u32 c.CompileFlag.SanAddress,
    };

    return out;
}

