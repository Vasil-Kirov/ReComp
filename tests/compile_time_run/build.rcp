module build
#import compile as c

compile :: fn() -> c.CompileInfo {
    out := c.CompileInfo {
        files = []string { "compile_time_run.rcp" },
        opt = 0,
        flags = .SanAddress,
    };
    return out;
}
