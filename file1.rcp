main

#import file2 as f2

struct x {
	foo: f2.x,
	bar: string,
}

my_64_fn :: fn() -> i32 {
	return 64;
}

my_16_fn :: fn(f: fn(x: i32) -> i32) -> i32 {
	return f(64);
}

main :: fn() -> i32 {
	// my_lambda := fn() -> i32 {
	// 	return 32;
	// };

	// fn_ptr := my_16_fn;
	
	//fn_ptr = my_64_fn;

	foo: i32 = 0;
	
	x := "my_f";

	return @i32 (#size x - #size foo);
}

