build
#import compile as c

compile :: fn() -> c.CompileInfo {
    out := c.CompileInfo {
        files = []string { "immutable_args.rcp" },
        opt = 0,
        flags = .SanAddress,
    };
    return out;
}
